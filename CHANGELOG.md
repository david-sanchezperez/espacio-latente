# Changelog

Formato basado en [Keep a Changelog](https://keepachangelog.com/es-ES/1.1.0/).

## [Unreleased]

### Fixed

- README: documentado `worker-buzon/` (Worker del chat del buzón de
  sugerencias), que existía en el repo y tenía su propio script
  `npm run desplegar:buzon` pero no aparecía en la tabla de "Qué hay aquí"
  ni tenía instrucciones de despliegue/secrets, a diferencia de `worker/` y
  `worker-radar/`.
